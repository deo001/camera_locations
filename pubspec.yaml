name: camera_locations
description: "App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.3
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  exif: ^3.0.0
  geolocator: ^13.0.0
  image: ^4.9.1
  geocoding: ^4.0.0
  intl: ^0.20.2
  camera: ^0.12.0+1
  gal: ^2.3.2
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true